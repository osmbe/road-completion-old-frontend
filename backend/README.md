### Before testing requests

Download necessary packages:
```❯ npm install```

You should run this command inside of the backend folder to start the local database:
```❯ ./script.sh```

To start testing requests run:
```❯ npm start```